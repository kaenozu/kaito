# Changelog

このプロジェクトの重要な変更を記録します。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) を参考にし、バージョン番号は Semantic Versioning に従います。

## [Unreleased]

### Added

- Windows上でパッケージ済みGUIを起動し、実ウィンドウとスクリーンショットを確認する`GUI acceptance` workflow
- 選択した空フォルダーをZIP内へ保持する処理

### Changed

- 公開Release・Production署名・SBOM・承認ゲート等の公開運用基盤を撤去し、個人利用向けに簡素化（公開リリースは行わない）
- 更新確認機能（GUIの更新確認ボタン・起動時チェック・設定項目・`update_checker`モジュール）を撤去
- コンソールスクリプトを診断CLIとExplorerガードを持つ`kaito.__main__:main`へ統一
- CIで`uv lock --check`を必須化
- 個人利用向けの内部開発版として`0.12.0.dev0`を使用

### Fixed

- プレリリースのバージョン比較
- 展開パスワード入力のマスクと値の破棄
- 診断レポートにおけるパスワードと絶対パスの除外
- 安全診断で拒否した後に解凍操作が再有効化される問題
- 安全診断で拒否したアーカイブの選択解凍
- 最近のファイル履歴削除
- 7z／RARプレビューによるTkイベントスレッド停止
- 画像プレビューの画素数上限
- Windows reparse pointを含むZIP作成
- コンテキストメニュー実装の二重化

## [0.11.0] - 2026-07-12

### Added

- 展開前に危険パス、リンク、上限超過、実行可能ファイル、二重拡張子を可視化する安全診断
- ZIP、RAR、7zを展開せず検査するCRC・データ整合性テスト
- アーカイブ一覧で選択したファイル・フォルダーだけを展開する機能
- 部分一致とglobによる検索、および画像・文書・アーカイブ・実行ファイル・大容量・暗号化フィルター
- AES-256暗号化ZIPの作成
- GitHub Releasesを利用した更新通知と設定画面からの無効化
- 個人パス、エントリ名、パスワードを除外した診断レポートのクリップボードコピーと`--diagnostics` CLI
- `--test-archive` CLIとExplorerの「kaitoで整合性を検査」メニュー
- 任意のPFX証明書によるEXE・インストーラーのAuthenticode署名支援
- 既存タグ、CHANGELOG、作業ツリーを確認するリリース事前検査スクリプト

### Changed

- 単一ルートを持たないアーカイブはアーカイブ名フォルダーへ展開し、既存フォルダーがある場合は`名前 (2)`形式で安全に別名化
- 安定版Release Workflowでタグ対象が現在の`master` HEADと一致することを必須化
- Release WorkflowでCHANGELOGの対象バージョンと診断レポートの正常性を検証

### Fixed

- Explorerレジストリの再帰削除に必要なアクセス権が不足していた問題
- 引用符付きで空白を含むパスワードが診断へ漏れる可能性
- 選択フォルダー展開時にアーカイブ項目数と選択数の積で処理量が増える問題
- 選択メンバー判定でループごとに集合を再生成していた問題
- ローカル署名時に`RUNNER_TEMP`が未定義だと失敗する問題

### Security

- 圧縮用パスワードをマスクされた確認入力ダイアログで受け取り、永続保存しない
- AES ZIPはZipCryptoではなくAES-256のみを使用
- 更新確認ではバージョン情報だけをGitHubへ問い合わせ、アーカイブ名・内容・設定を送信しない

## [0.10.1] - 2026-07-12

`v0.10.0`は既存の履歴タグと競合していたため、このリリースは`v0.10.1`として公開します。

### Added

- ZIP、7z、RARの一覧取得・展開を統一するアーカイブサービス層
- ZIP、7zの圧縮機能
- 7-Zip 26.02の同梱と起動時整合性検証
- アーカイブ単位のパスワード管理
- 複数アーカイブの処理キュー
- 実行中処理のキャンセル
- `--version`、`--self-test`、`--backend-info --json`、`--archive-smoke --json` の診断CLI
- Inno Setupによるユーザー単位インストーラー
- Windowsエクスプローラーの解凍・圧縮コンテキストメニュー
- libarchive由来の実RARフィクスチャを使用した統合テスト
- Windows CIでのEXE、インストーラー、インストール・アンインストールE2E
- standalone版とインストール版の実アーカイブ操作スモークテスト

### Changed

- `patool`依存を廃止し、ZIP標準ライブラリと同梱7-Zip CLIへ移行
- frozen版ではシステム7-Zipへフォールバックせず、同梱版のみを使用
- AES-256を含む暗号化ZIPの展開・プレビューを同梱7-Zip経由へ変更
- 展開処理をステージングディレクトリ経由へ変更
- バージョン情報をパッケージメタデータへ一元化
- 設定ファイルの型・値域検証と原子的保存を追加
- GUIを日本語UIとして明示し、未実装だった言語切替設定を削除

### Security

- `..`、絶対パス、UNC、ドライブ指定、Windows予約名、ADSを含む危険パスを拒否
- シンボリックリンク、junction、reparse pointを含むアーカイブを拒否
- Windowsで衝突する大文字小文字違いと重複パスを拒否
- エントリ数、合計展開サイズ、単一ファイルサイズ、圧縮率、パス長の上限を強制
- 展開後の再検査により、展開先外への書き込みを防止
- ログ、例外、GUIメッセージにパスワードを出さないマスキング処理を追加
- 同梱7-Zipと取得元パッケージのSHA-256を固定・検証
- 既存の出力アーカイブをサービス層とExplorer右クリック圧縮の両方で無断上書きしない

### Fixed

- 複数ファイルのドラッグ&ドロップで先頭以外が失われる問題
- 展開先が`archive/archive`のように二重になる問題
- 間違ったパスワードを成功前にキャッシュする問題
- 7z暗号化圧縮時に通常処理ではパスワードが渡されない条件ミス
- キャンセル要求が実行中の7-Zipプロセスへ届かない問題
- RAR/7zプレビューでアーカイブ全体をGUIスレッド上へ展開する問題
- 設定JSONのルート型が不正な場合に起動できない問題
- 設定保存時の一時ファイルディスクリプタ処理
- 最近使ったファイルの表示用短縮文字列を実パスとして再利用する問題
- 処理中終了時のワーカースレッドとTkコールバック競合
- 暗号化ZIPをPython標準ZIP処理へ渡して展開・プレビューできない問題

### Known limitations

- RARの作成には対応しません。RARは一覧取得と展開のみです。
- 7-Zip CLIの制約により、暗号化アーカイブのパスワードは子プロセスのコマンドライン引数として渡されます。同一ユーザー権限の別プロセスから閲覧される可能性があります。
- Windowsコード署名は未導入のため、ダウンロードしたEXEまたはインストーラーでSmartScreen警告が表示される場合があります。

[Unreleased]: https://github.com/kaenozu/kaito/compare/v0.11.0...HEAD
[0.11.0]: https://github.com/kaenozu/kaito/compare/v0.10.1...v0.11.0
[0.10.1]: https://github.com/kaenozu/kaito/compare/v0.9.3...v0.10.1
